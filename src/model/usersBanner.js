const { execQuery } = require("../../dependencies/oracleConnect");


const getFacultyRoles = async () => {

    const qry = `insert query`;         

    return await execQuery(qry)
}

module.exports = { getFacultyRoles }